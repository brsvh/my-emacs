;;; minions.el --- minions configuration -*- lexical-binding: t; -*-

;; ... :)
(setq minions-mode-line-lighter ":)"
      minions-mode-line-delimiters '("" . ""))

(add-hook 'emacs-startup-hook 'minions-mode)

;;; minions.el ends here
